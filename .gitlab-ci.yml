image: gcc

before_script:
    - apt update && apt install -y cmake libhdf5-serial-dev

tests:
    only:
        - pushes
    script:
        - mkdir build
        - cd build
        - cmake ..
        - make -j4
        - ./tests
